# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Find the latest Ruff version at
  # https://github.com/astral-sh/ruff-pre-commit/releases
  rev: v0.4.4 # Use a recent stable ruff version
  hooks:
    # Run the linter (isort, flake8, pyupgrade, etc.)
    - id: ruff
      args: [--fix, --exit-non-zero-on-fix]
    # Run the formatter (replaces Black)
    - id: ruff-format
# Uncomment and configure MyPy if/when ready for stricter type checking
#- repo: https://github.com/pre-commit/mirrors-mypy
#  # Find the latest MyPy version
#  rev: v1.9.0
#  hooks:
#    - id: mypy
#      # Adjust additional_dependencies if your project needs specific typeshed stubs
#      # additional_dependencies: [ "types-requests" ]
